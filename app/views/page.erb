<header class="header">
    <h1><span class="keyword">class</span> <a href="<%= @settings.url %>" title="Главная">Homepage</a></h1>
</header>
<section class="main">
    <article>
        <header>
            <h2><%= @page.title %></h2>
        </header>
        <%= @page.content %>
    </article>
</section> <!-- .main -->
